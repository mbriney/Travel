# Local OAuth credentials — never commit
tools/credentials.json

# Raw TripIt API dumps (large, not needed in repo)
data/raw/

# AeroDataBox per-flight cache (local-only — saves API quota across reruns)
data/_aerodatabox_cache/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editor
.DS_Store
.idea/
.vscode/
node_modules/
